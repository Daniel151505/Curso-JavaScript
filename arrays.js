// Declarando arreglo
var frutas = [
    "Manzana", "Platano", "Cereza", "Fresa"
];

// Longitud
console.log(frutad.length);

// Posición del elemento
console.log(frutas[0]);

// Agregando elemento
var masFrutas = frutas.push("Uvas");

// Eliminando elemento
var ultimo = frutas.pop("Uvas");

// Agregando elemento al comienzo
var nuevaLongitud = frutas.unshift("Pera");

// Eliminando elemento del comienzo
var borrarFruta = fruta.length.shift("Pera");

// Posición del elemento
var posicion = frutas.indexOf("Cereza");

